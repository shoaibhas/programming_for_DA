[project]
name = "programming-for-da"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "shoaibhas", email = "118734608+shoaibhas@users.noreply.github.com" }
]
requires-python = ">=3.14"
dependencies = [
    "jupyter>=1.1.1",
    "matplotlib>=3.11.2",
    "pandas>=3.0.6",
    "seaborn>=0.13.2",
]

[project.scripts]
programming-for-da = "programming_for_da:main"

[build-system]
requires = ["uv_build>=0.12.16,<0.13.0"]
build-backend = "uv_build"
